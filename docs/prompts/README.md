# Prompts (Codex / AI)

This folder contains prompt guides/templates for AI-assisted development.

Important:
- The public keyword contract is defined in `docs/KEYWORDS.md` and `docs/SPECIFICATION.md`.
- Prompts MUST NOT introduce new keyword names.
- Any change must keep `tests/contract/contract.robot` passing.
