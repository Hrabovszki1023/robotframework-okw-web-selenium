# Testing

## Contract tests
Run:
- `robot tests/contract`

Contract tests enforce the public keyword set and prevent accidental API drift.

## Notes
Functional UI tests may require a demo application and driver configuration.
